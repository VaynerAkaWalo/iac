apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: elastic-cloud
  namespace: flux-system
spec:
  interval: 3m0s
  path: ./infrastructure/elastic-cloud
  dependsOn:
    - name: namespaces
    - name: secrets
  sourceRef:
    kind: GitRepository
    name: flux-system
  prune: true
  wait: true
