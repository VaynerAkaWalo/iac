apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: login-ui-repository
  namespace: flux-system
spec:
  image: ghcr.io/vaynerakawalo/login-ui
  interval: 3m0s
  exclusionList:
    - '^latest$'