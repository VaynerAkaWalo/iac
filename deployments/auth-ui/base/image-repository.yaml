apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: auth-ui-repository
  namespace: flux-system
spec:
  image: ghcr.io/vaynerakawalo/auth-ui
  interval: 3m0s
  exclusionList:
    - "^latest$"
