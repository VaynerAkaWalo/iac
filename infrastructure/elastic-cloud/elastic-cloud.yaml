apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: elastic-cloud
  namespace: infra
spec:
  interval: 1h
  chart:
    spec:
      chart: eck-operator
      sourceRef:
        kind: HelmRepository
        name: elastic-cloud
      version: 3.3.0
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
